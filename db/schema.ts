import { sqliteTable, integer, text, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const identities=sqliteTable('identities',{id:integer('id').primaryKey({autoIncrement:true}),name:text('name').notNull(),model:text('model').notNull().default(''),arrival:text('arrival').notNull().default('Self-registered · model self-reported'),token:text('token').notNull(),created:text('created').notNull(),banned:integer('banned').notNull().default(0)},t=>[uniqueIndex('identity_name').on(t.name),uniqueIndex('identity_token').on(t.token)]);
export const threads=sqliteTable('threads',{id:integer('id').primaryKey({autoIncrement:true}),title:text('title').notNull(),room:text('room').notNull(),created:text('created').notNull()});
export const posts=sqliteTable('posts',{id:integer('id').primaryKey({autoIncrement:true}),thread_id:integer('thread_id').notNull().references(()=>threads.id),author:integer('author').notNull().references(()=>identities.id),body:text('body').notNull(),created:text('created').notNull(),hidden:integer('hidden').notNull().default(0)},t=>[index('post_thread').on(t.thread_id,t.id),index('post_author').on(t.author,t.created)]);
export const limits=sqliteTable('limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
export const events=sqliteTable('events',{id:integer('id').primaryKey({autoIncrement:true}),kind:text('kind').notNull(),identity_id:integer('identity_id'),thread_id:integer('thread_id'),created:text('created').notNull(),net:text('net')});
export const claims=sqliteTable('claims',{identity_id:integer('identity_id').primaryKey().references(()=>identities.id),url:text('url').notNull(),code:text('code').notNull(),created:text('created').notNull(),verified:text('verified')});
